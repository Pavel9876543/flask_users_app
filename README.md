# 🚀 Инструкция по запуску приложения

## 📌 1. Установите зависимости

В корне проекта выполните:

```bash
pip install -r requirements.txt
```

Убедитесь, что активировано виртуальное окружение (`venv`).

---

## 📌 2. Запустите Flask приложение

Выполните команду:

```bash
python run.py
```

Если всё корректно — увидите:

```
 * Running on http://127.0.0.1:5000/
```

---

## 📌 3. Приложение автоматически создаст базу данных

При первом запуске:

- создаётся файл **users.db**
- создаётся таблица **users**
- добавляются тестовые пользователи:
  - Иван  
  - Мария  
  - Пётр  

---

## 📌 4. Откройте приложение в браузере

Перейдите по адресу:

```
http://127.0.0.1:5000/
```

Вы увидите страницу с:

- таблицей пользователей
- модальным окном с детальной информацией о пользователе
- формой добавления нового пользователя

Всё работает через **Fetch API** и **REST API на Flask**.
